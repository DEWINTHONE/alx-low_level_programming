#include <stdio.h>

/**
 * main - prints the alphabet in lowercase
 * Return: will return back to 0
 */

int main(void)
{
	char i;

	for (f = 'a'; i <= 'z'; i++)
		putchar(i);
		putchar ('\n');
	return (0);
}
